# Terraform working files — never commit these
# (comments must be on their own line — inline comments are NOT valid in .gitignore)
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars
# .terraform.lock.hcl IS committed on purpose (pins provider versions)
